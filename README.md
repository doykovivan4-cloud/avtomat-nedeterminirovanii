# Практическая работа: Конечный автомат Недетерминированный 

## 1. Тип конечного автомата

### Конечный недетерминированный автомат (НКА) — математическая модель вычислений, обобщающая детерминированные конечные автоматы (ДКА).
Его ключевая особенность: для одного и того же входного символа из текущего состояния может быть несколько возможных переходов (или даже ни одного).

## 2. Компоненты автомата
Σ — входной алфавит: конечное множество символов, которые автомат может считывать на входе (например, Σ={a,b}).

Q — множество состояний: конечное множество внутренних состояний автомата (например, Q={q 
0
​
 ,q 
1
​
 ,q 
2
​
 }).

q 
0
​
 ∈Q — начальное состояние: одно выделенное состояние, с которого начинается работа автомата.

F⊆Q — множество заключительных (принимающих) состояний: подмножество состояний, попадание в любое из которых означает успешное распознавание входной строки.

Φ — функция переходов: отображение Q×(Σ∪{ε})→2 
Q
 , задающее возможные переходы:

Φ(q,a) — множество состояний, в которые автомат может перейти из состояния q при чтении символа a∈Σ;

Φ(q,ε) — множество состояний, в которые автомат может перейти без чтения входного символа (так называемые ε-переходы, или пустые переходы).

## 3. таблица переходов
| Текущее состояние | Входной символ | Множество следующих состояний |
|----------------|--------------|--------------------------|
| $q_0$         | $a$          | $\{q_1\}$               |
| $q_0$         | $b$          | $\varnothing$ (нет перехода) |
| $q_1$         | $a$          | $\{q_1\}$               |
| $q_1$         | $b$          | $\{q_1, q_2\}$         |
| $q_2$         | $a$          | $\varnothing$ (нет перехода) |
| $q_2$         | $b$          | $\varnothing$ (нет перехода) |
